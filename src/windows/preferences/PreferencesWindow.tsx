import { WindowShell } from "../shared/WindowShell";

export function PreferencesWindow() {
  return (
    <WindowShell
      title="OmniTranslate 设置"
      description="OCR / ASR / 翻译三大引擎模块化配置（UI 骨架）。"
      placeholder="Phase 2：Tab 导航与 Provider 表单。"
    />
  );
}
